diff --git a/node_modules/metro/src/Assets.js b/node_modules/metro/src/Assets.js
index b34df88..f0f97d3 100644
--- a/node_modules/metro/src/Assets.js
+++ b/node_modules/metro/src/Assets.js
@@ -171,9 +171,9 @@ async function getAssetData(
   assetUrlPath = (0, _pathUtils.normalizePathSeparatorsToPosix)(assetUrlPath);
   const isImage = isAssetTypeAnImage(_path.default.extname(assetPath).slice(1));
   const assetInfo = await getAbsoluteAssetInfo(assetPath, platform ?? null);
-  const isImageInput = assetInfo.files[0].includes(".zip/")
-    ? _fs.default.readFileSync(assetInfo.files[0])
-    : assetInfo.files[0];
+  // image-size >=2.x only accepts a Buffer/Uint8Array, so read the file before
+  // calling imageSize().
+  const isImageInput = _fs.default.readFileSync(assetInfo.files[0]);
   const dimensions = isImage ? (0, _imageSize.default)(isImageInput) : null;
   const scale = assetInfo.scales[0];
   const assetData = {
